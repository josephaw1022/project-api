# Agent Instructions

Welcome to the `project-api` repository. This repository implements a custom Kubernetes aggregated API server that brings the OpenShift "Project" concept to vanilla Kubernetes clusters.

## Agent Guidelines

- **Context:** All repository-specific context, rules, workflows, and skills are stored in the `.agents/` directory.
