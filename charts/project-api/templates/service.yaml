apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-api.fullname" . }}
  labels:
    {{- include "project-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 443
      protocol: TCP
      name: https
  selector:
    {{- include "project-api.selectorLabels" . | nindent 4 }}
